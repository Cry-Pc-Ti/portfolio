import { Typography } from '@mui/material';

const NotionMasterBotDetail: React.FC = () => {
  return (
    <div>
      <Typography variant="h6">Notion Master Bot</Typography>
      <Typography variant="body1">Connect Notion On Discord</Typography>
      {/* ここに詳細ページの内容を追加 */}
    </div>
  );
};

export default NotionMasterBotDetail;
